/// A struct
struct Foo {
    /// A primitive field
    foo: u32,

    /// A field containing another struct
    bar: Bar,

    /// A field containing an abstract type
    baz: const * Baz,
}

struct Bar {
    bar: owned * Baz,
}

struct Baz;
